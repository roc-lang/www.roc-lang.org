## Project instructions

See `README.md` for build and preview commands.

This repository uses the new Zig-based Roc compiler pinned in `website/build_website.roc` (the `roc:` field in the app header and the `roc_version` constant).
